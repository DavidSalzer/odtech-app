odtechApp.controller('header', ['$scope', '$rootScope', '$state', 'server', '$timeout', 'camera', function ($scope, $rootScope, $state, server, $timeout, camera) {
    //set the state - for hide and show the footer
    //log out, it here temporarly
    $scope.$state = $state;
  
   
} ]);

